spring:
  cloud:
    discovery:
      client:
        simple:
          instances:
            test:
              - uri: http://localhost:8091
                metadata:
                  management.context-path: /actuator